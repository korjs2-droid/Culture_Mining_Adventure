# Retro Invaders

Space Invaders 스타일의 고전게임입니다.

## 실행 방법

1. 이 폴더에서 터미널 실행
2. 아래 명령으로 정적 서버 실행

```bash
python3 -m http.server 8000
```

3. 브라우저에서 `http://localhost:8000` 접속

## 조작법

- `←` `→`: 이동
- `Space`: 발사
- `P`: 일시정지
- `R`: 재시작
